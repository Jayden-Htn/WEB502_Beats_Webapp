App As appinfo:
    BackEnabled: =true
    OnStart: |-
        =Set(varUser, Blank());
        Set(varID, Blank());
        Clear(varChildren);
    Theme: =Beats

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

