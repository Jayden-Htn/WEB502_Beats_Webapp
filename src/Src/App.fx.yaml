App As appinfo:
    BackEnabled: =true
    OnStart: |-
        =Set(varUser, Blank());
        Set(varID, Blank());
        Set(varAuthorised, false);
    Theme: =Beats

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

