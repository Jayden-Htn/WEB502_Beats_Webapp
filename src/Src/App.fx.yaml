App As appinfo:
    BackEnabled: =true
    OnStart: =Set(varAuthorised, false)
    Theme: =Beats

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

